---
# Agent Role - Main Tasks
# 此文件作为参考，实际逻辑在 playbooks/deploy-agent.yml 中
# 保留此文件是为了未来可以将任务拆分成更细的模块

- name: Include deployment variables
  ansible.builtin.import_tasks: tasks/include_vars.yml
  tags: always
