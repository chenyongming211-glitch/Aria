# SD-WAN Agent Environment Configuration
# This file is sourced by systemd when starting the agent

# Controller URL (required)
# For high availability, use a load balancer or specify backup URLs in agent.yaml
CONTROLLER_URL=http://localhost:8080

# Log level: debug, info, warn, error
LOG_LEVEL=info
